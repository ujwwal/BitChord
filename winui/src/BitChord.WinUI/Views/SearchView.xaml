<UserControl
    x:Class="BitChord.WinUI.Views.SearchView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="using:BitChord.WinUI">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <AutoSuggestBox x:Name="SearchBox"
                        Grid.Row="0"
                        Height="46"
                        Margin="10,0,10,4"
                        Padding="8,0,12,0"
                        Background="{ThemeResource SurfaceVariantBrush}"
                        BorderThickness="0"
                        CornerRadius="11"
                        FontFamily="{StaticResource RegularFont}"
                        FontSize="16"
                        PlaceholderText="Search">
            <AutoSuggestBox.QueryIcon>
                <FontIcon Glyph="&#xE721;" FontSize="18" />
            </AutoSuggestBox.QueryIcon>
        </AutoSuggestBox>

        <ItemsControl x:Name="FilterHost"
                      Grid.Row="1"
                      Margin="10,4,10,8">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal" Spacing="8" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate x:DataType="vm:SearchFilterOption">
                    <Border Padding="10,6"
                            CornerRadius="14"
                            Background="{ThemeResource SurfaceVariantBrush}">
                        <TextBlock Text="{x:Bind Label}"
                                   Style="{StaticResource TitleStyle}"
                                   Foreground="{ThemeResource PrimaryTextBrush}" />
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <ListView x:Name="ResultsList"
                  Grid.Row="2"
                  Margin="10,8,10,0"
                  SelectionMode="None"
                  IsItemClickEnabled="True">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="vm:SearchResultTile">
                    <Grid Padding="8,10" ColumnSpacing="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Border Width="32" Height="32" Background="{ThemeResource SurfaceVariantBrush}" CornerRadius="8">
                            <FontIcon Glyph="&#xE8B7;" FontSize="16" Foreground="{StaticResource AccentBrush}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="{x:Bind Title}" Style="{StaticResource TitleStyle}" TextTrimming="CharacterEllipsis" />
                            <TextBlock Text="{x:Bind Subtitle}" Style="{StaticResource BodyStyle}" TextTrimming="CharacterEllipsis" />
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
